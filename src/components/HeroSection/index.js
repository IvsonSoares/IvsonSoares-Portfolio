import React from "react";
import {HeroContainer,
        HeroBg,
        HeroContent

} from "./HeroElements";

const HeroSection = () => {

    return (
        <HeroContainer>
            <HeroBg>
            </HeroBg>
            <HeroContent>
            </HeroContent>
          </HeroContainer>
    )
}

export default HeroSection